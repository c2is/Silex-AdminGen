<div class="navbar">
    <div class="navbar-inner">
        <a class="brand" href="#">Admin</a>
        <ul class="nav">
            {% for item in app.admingen_menu %}
            <li><a href="{{ item.url }}">{{ item.name | trans }}</a></li>
            {% endfor %}
        </ul>
    </div>
</div>
